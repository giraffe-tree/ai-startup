---
name: ai-startup
description: AI 原生创业全流程陪伴 skill。按《创始人手册》引导用户走完创意→MVP→发布→规模化四个阶段，每个阶段的关键文档都持久化到本地 startup/ 文件夹，随时可中断、续接。当用户说"开始创业"、"我有个创业想法"、"继续创业"、"进入MVP阶段"、"帮我验证想法"、"创业旅程"、"AI原生创业"、"帮我做创业规划"等时触发。即使用户只是说"有个想法想聊聊"或"想做个产品"也应触发此 skill。
---

# AI 原生创业旅程

陪伴创始人走完 AI 原生创业的四个阶段：**创意 → MVP → 发布 → 规模化**。

所有阶段文档持久化到当前工作目录的 `startup/` 文件夹，每次 session 可从上次中断处继续。

---

## 第一步：Session 开始时必做

读取 `startup/state.md`（如果存在）。

```bash
# 检查是否有已有项目
ls startup/state.md 2>/dev/null
```

- **文件存在** → 读取内容，向用户汇报当前阶段和进度，询问今天想继续做什么
- **文件不存在** → 这是新项目，询问项目名称，然后创建 `startup/` 目录结构，从创意阶段开始

初始化目录结构：

```bash
mkdir -p startup/创意阶段 startup/MVP阶段 startup/发布阶段 startup/规模化阶段
```

---

## 文档结构

```
startup/
├── state.md              ← 每次 session 必读/必写，记录当前阶段和关键决策
├── 创意阶段/
│   ├── problem.md        ← 可测试的问题假设
│   ├── market.md         ← 竞争格局 + TAM/SAM/SOM
│   ├── interviews.md     ← 客户访谈记录与主题综合
│   └── solution.md       ← 解决方案概念 + 原型描述
├── MVP阶段/
│   ├── CLAUDE.md         ← 架构上下文（放入代码仓库根目录）
│   ├── scope.md          ← MVP 范围（做/不做/修改标准）
│   ├── metrics.md        ← PMF 度量框架
│   └── session-log.md    ← 构建会话日志（追加）
├── 发布阶段/
│   ├── tech-debt.md      ← 技术债审计 + 修复排期
│   ├── operations.md     ← 运营审计 + 自动化设计
│   └── compliance.md     ← 安全合规清单
└── 规模化阶段/
    ├── channels.md       ← 增长渠道 + 单元经济学
    └── scale-plan.md     ← 规模化路线图
```

---

## state.md 格式（每次 session 结束前更新）

```markdown
# 创业项目状态

**项目名称**: {名称}
**当前阶段**: 创意阶段 | MVP阶段 | 发布阶段 | 规模化阶段
**最近更新**: YYYY-MM-DD

## 已完成
- [x] 问题假设
- [ ] 市场研究
...

## 当前任务
{下一步具体要做的事}

## 关键决策
- 目标用户: {描述}
- 核心问题假设: {一句话}
- 当前阶段退出条件: {具体指标或证据}
```

---

## 各阶段引导

### 创意阶段

**目标**：以研究为导向的验证——证明真实问题存在且你的方案能解决它，再写第一行代码。

按顺序完成以下任务，每完成一个保存文档并更新 `state.md`：

#### 1. 打磨问题假设 → `startup/创意阶段/problem.md`

引导用户将模糊观察转化为可测试假设：
- 谁（具体职位/角色）有这个问题？
- 多频繁？严重程度？
- 目前如何应对？
- 测试：假设能被证伪吗？

**对抗确认偏误**：用 Claude 寻找反驳证据，而不只是支持证据。

保存格式：
```markdown
# 问题假设

**版本**: v{n}  **日期**: YYYY-MM-DD

## 可测试假设
{具体陈述，包含：谁/多频繁/多严重/当前如何应对}

## 支持证据
{...}

## 反驳证据
{...}

## 待验证的最强反驳
{...}
```

#### 2. 市场研究 → `startup/创意阶段/market.md`

- 竞争格局分层（直接/间接/相邻）
- 竞争对手客户的反复投诉（免费定性研究）
- TAM/SAM/SOM 估算 + 假设压力测试
- 3个外部趋势评估（监管/技术/人口结构，顺风/逆风）

#### 3. 客户探索 → `startup/创意阶段/interviews.md`

引导用户规划并记录客户访谈：
- 目标画像（职位/公司类型/资历）
- 访谈框架（过去行为问题，不问假设性未来）
- 每5次访谈后综合：支持假设的证据 vs 挑战假设的证据

访谈记录格式：
```markdown
# 访谈记录

## 访谈 {n} — {日期}
**受访者**: {职位/背景}

**关键发现**:
- 支持假设: ...
- 挑战假设: ...
- 意外发现: ...

## 阶段综合（每5次更新）
**最强信号**: ...
**最大挑战**: ...
```

#### 4. 解决方案概念 → `startup/创意阶段/solution.md`

- 解决方案概念（基于验证而非最初假设）
- 核心交互设计
- 依赖的3个关键假设 + 每个假设成立的条件
- 用 Claude Code 构建轻量级原型（单一核心交互）

#### 创意阶段退出检查

向用户逐一确认：
1. 问题是真实且具体的吗？（能准确说出谁/频率/严重程度/当前应对）
2. 解决方案能解决验证过程揭示的实际问题吗？
3. 是否有足够信号（定性证据）支撑开始构建？

三项均确认 → 更新 `state.md`，进入 MVP 阶段。

---

### MVP 阶段

**目标**：将验证过的问题转化为真实用户会使用的可运行产品，同时建立可维护的架构基础。

#### 1. 架构上下文文档 → `startup/MVP阶段/CLAUDE.md`

在写第一行代码前，用 Claude 定义并记录架构决策：
- 技术栈选择与原因
- 要遵循的模式，要避免的依赖
- 6个月内预期的规模约束
- 有意接受的权衡

**重要**：提醒用户将此文件复制到代码仓库根目录——每个 Claude Code 会话都依赖它保持架构一致性。

#### 2. MVP 范围文档 → `startup/MVP阶段/scope.md`

```markdown
# MVP 范围

## 产品做什么
{核心功能列表}

## 产品刻意不做什么
{明确排除项}

## 功能修改标准
添加新功能需要满足：{具体的来自真实用户的证据标准}

## 核心用户旅程
{单一最重要的用户流程}
```

#### 3. 度量框架 → `startup/MVP阶段/metrics.md`

在第一个用户出现前定义：
- 留存基准（第7天/第30天目标）
- 激活标准（用户完成什么算激活）
- PMF 指标（Sean Ellis 测试基准：>40% "非常失望"）
- 假阳性定义（没有激活的注册、没有留存的收入）

#### 4. 构建会话日志 → `startup/MVP阶段/session-log.md`（追加）

每次 Claude Code 构建会话结束后追加：
```markdown
## 会话 {n} — {日期}

**本次完成**: {构建了什么}
**关键决策**: {做出了哪些架构/产品决策}
**引入的假设**: {哪些假设需要后续验证}
**下次继续**: {具体任务}
```

#### 5. 上线前安全审查 checklist

确认以下项目在第一个真实用户接触前完成：
- [ ] 身份验证和会话处理
- [ ] API 响应中的数据暴露
- [ ] 输入验证和注入风险
- [ ] 已知漏洞的依赖项
- [ ] 任何涉及认证/机密/数据处理的代码已人工审查

#### MVP 阶段退出检查

- [ ] 真实用户在使用产品（不只是创始人的朋友）
- [ ] 有留存数据（用户回来了）
- [ ] Sean Ellis 测试 > 40%，或有收入/口碑信号
- [ ] 努力转变：产品开始"拉"用户而非创始人"推"

三轮迭代后仍无进展 → 运行诊断（细分/定位/产品问题）再决定调整/转向/回创意阶段。

---

### 发布阶段

**目标**：将早期牵引力转化为可重复、可持续的增长引擎，构建解放创始人注意力的运营系统。

#### 1. 技术债审计 → `startup/发布阶段/tech-debt.md`

用 Claude Code 审计 MVP 代码库：
- 结构性弱点
- 测试覆盖率缺口
- 重构候选

排期修复：
- P0（下个版本前必须修复）
- P1（可与功能开发并行）
- P2（可接受的持续债务）

同步将 MVP 阶段的脑海中的架构决策补录到 `CLAUDE.md`。

#### 2. 运营审计 → `startup/发布阶段/operations.md`

审计创始人当前所有运营负荷，分类：
- **可完全自动化**：设计工作流逻辑（触发条件/决策规则/输出/去向）
- **需要人但不必是创始人**：可委托的任务
- **真正需要创始人判断**：保留这些

#### 3. 安全合规清单 → `startup/发布阶段/compliance.md`

针对目标市场所需框架（SOC 2 / GDPR / HIPAA）：
- 代码层面漏洞和合规差距
- 企业买家会要求的控制措施、审计日志、访问管理
- 优先修复序列

#### 发布阶段退出检查

- [ ] 增长可重复且渠道驱动（CAC/LTV/回本期可量化）
- [ ] 基础设施能处理生产工作负载
- [ ] 运营不依赖创始人作为瓶颈

---

### 规模化阶段

**目标**：在保持产品-市场契合的基础上，构建可持续扩张的组织与增长系统。

#### 1. 增长渠道 → `startup/规模化阶段/channels.md`

- 各渠道单元经济学（CAC/LTV/回本期）
- 渠道优先级排序
- 防止过早扩张：新市场验证标准

#### 2. 规模化路线图 → `startup/规模化阶段/scale-plan.md`

- 团队扩张节奏（什么阶段招什么人）
- 融资时机判断
- 组织系统设计（从创始人中心到系统驱动）

---

## 核心原则（贯穿所有阶段）

1. **先验证再构建**：原型是对话道具，不是验证证据
2. **文档驱动续接**：每次 session 结束前更新 `state.md`
3. **对抗确认偏误**：始终让 Claude 找反驳证据，不只是支持证据
4. **退出标准驱动**：每个阶段有明确的退出条件，不靠感觉判断
5. **CLAUDE.md 是命脉**：架构上下文文档是 AI 协作的持久记忆
